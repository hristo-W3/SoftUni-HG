try {
    // Code that can throw an exception
    // Some other code - not executed in case of error!
} catch (ex) {
    // This code is exdcuted in case of exception
    // ex holds the info about the exception
}